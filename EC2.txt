Comandos para conectar na EC2 via SSH.

Instance ID
EXEMPLO - XPTO
    Open an SSH client.

    Locate your private key file. The key used to launch this instance is key.pem -> ache o local onde esta a chave 

    Run this command, if necessary, to ensure your key is not publicly viewable.

 chmod 400 devops.pem -> permissao para a key.pem

Connect to your instance using its Public DNS:

     ec2-100-25-44-243.compute-1.amazonaws.com 

Example:
ssh -i "key.pem" ubuntu@ec2-100-25-44-243.compute-1.amazonaws.com -> exemplo de como conectar na EC2 via SSH